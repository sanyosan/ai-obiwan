# 未来創造家 勤怠アプリ

出退勤の**予定**を登録し、**予定どおりに出勤・退勤できたか**を記録する自作アプリ。
スマホのホーム画面に置けば普通のアプリのように使える（PWA）。パソコンのブラウザからも同じものが使える。

```
  スマホ / PC                      Google Apps Script            スプレッドシート
 ┌──────────────┐   HTTPS   ┌──────────────────┐          ┌────────────────┐
 │  kintai/     │ ────────▶ │  attendance-app/gas │ ───────▶ │ 社員マスター     │
 │  （PWA）      │           │  ・シフトの展開      │          │ シフト区分       │
 │ 打刻/シフト/統計│ ◀──────── │  ・打刻の判定・通知   │          │ 勤務予定/出退勤   │
 └──────────────┘           │  ・未打刻の見張り     │          │ 端末 / 通知ログ  │
                            └──────────────────┘          └────────────────┘
                                     │
                                     └─▶ メール・Slack等へ通知
```

- `kintai/` … スマホアプリの本体（HTML/CSS/JS だけ。どこに置いても動く）
- `attendance-app/gas/` … Google Apps Script のコード
- `attendance-app/test/` … サーバー側の動作確認（Node で動く）

---

## できること

**社員**

- **月間予定**を1画面で確認（カレンダーにシフト記号、出勤日数・休み・予定の実働）
- 出勤予定・退勤予定をカレンダーで**まとめて登録**（「今月の平日を全部 9:00-18:00」がボタン1つ）
- 早番・日勤・遅番などの**シフト区分を選ぶだけ**で時刻と勤務形態が入る
- 出勤／退勤の打刻。**「出社（GPS）／リモート／外出・直行直帰」を選べる**
- 出社を選ぶと位置情報が必須になり、緯度経度・誤差・住所・事業所からの距離を記録
- 打刻に使った端末（端末IDと端末名）も記録
- 自分の履歴と月次の統計を見る
- PINの変更

**管理者**

- **月間シフト表**（人 × 日のマス目）でシフトを組む。マスを押すだけ
- シフト区分（早番・遅番・夜勤…）の追加・編集。時刻・休憩・勤務形態・色を決められる
- 全員の履歴・統計を見る（メンバー別の一覧表つき）
- **社員の追加・編集・退社処理**（増えても減っても画面から対応できる）
- 他の人の予定を代理で登録
- 事業所の座標・遅刻猶予・通知のON/OFFなどの設定
- 登録された端末の一覧

**自動で届く通知**

| いつ | 内容 |
|---|---|
| 出勤・退勤の打刻をしたとき | 誰が何時に、どの勤務形態で、予定どおりか遅刻か、場所と端末 |
| 勤務予定を登録・変更・削除したとき | 対象日と時間 |
| シフトを組んだ・変えたとき | 対象者に対象日をまとめて通知 |
| 出勤予定を過ぎても打刻がないとき | 未出勤アラート（10分おきに見張る） |
| 退勤予定を大きく過ぎても打刻がないとき | 未退勤アラート |
| 打刻がないまま日をまたいだとき | その日の締めで欠勤にして通知 |

宛先は**本人のメール**と**管理者全員のメール**。Slack / Google Chat / Discord の Webhook にも同時に流せる。

---

## セットアップ

### A. Apps Script にコードを入れる

やり方は2つ。**ブラウザだけで完結させたいなら A-1**、
コマンドが使えるなら A-2 のほうが以後の更新が楽。

#### A-1. Chrome だけで入れる（貼り付け1回・おすすめ）

11ファイルを貼って回らなくていいように、
**全部をつなげた1枚** `attendance-app/paste-to-gas.gs` を用意してある。

1. <https://script.google.com/> を開いて「新しいプロジェクト」
2. プロジェクト名を「未来創造家 勤怠」などにする
3. 左に出ている `コード.gs` を開き、**中身を全部消す**
4. `attendance-app/paste-to-gas.gs` の中身を**まるごと貼り付けて保存**（Ctrl/⌘+S）
5. 左の歯車（プロジェクトの設定）で **タイムゾーンが「(GMT+09:00) 日本標準時」** になっているか確認。
   違っていたら直す（日付の区切りがずれるため）
6. 上の関数選択で **`setup`** を選び「実行」。初回は権限の確認が出るので許可する
7. 実行ログに出る**スプレッドシートのURL・アプリキー・各人のPIN**を控える
8. 右上「デプロイ」→「新しいデプロイ」→ 歯車で**ウェブアプリ**を選ぶ
   - 次のユーザーとして実行: **自分**
   - アクセスできるユーザー: **全員**
9. 出てきた `https://script.google.com/macros/s/.../exec` を控える

> コードを直したときは、`paste-to-gas.gs` を貼り直したうえで
> **「デプロイを管理」→ 鉛筆 → バージョン「新バージョン」→ デプロイ**。URLは変わらない。

`paste-to-gas.gs` は `gas/` から自動生成している。中身を直すときは `gas/` を直して
`node attendance-app/tools/bundle.js` で作り直す（テストがズレを検出する）。

#### A-2. clasp を使う（コマンドが使えるなら・以後が楽）

事前にひとつだけ。<https://script.google.com/home/usersettings> を開いて
**「Google Apps Script API」をオンにする**。ここがオフだと `push` が必ず失敗する。

```bash
cd attendance-app

# 初回だけ。ブラウザが開いてGoogleログインを求められる
npx @google/clasp login

# Apps Script プロジェクトを新規に作る（.clasp.json が自動でできる）
npx @google/clasp create --type standalone --title "未来創造家 勤怠" --rootDir ./gas

# コードを流し込む
npx @google/clasp push -f
```

すでにブラウザ側でプロジェクトを作ってある場合は `create` の代わりに、
`.clasp.json.sample` を `.clasp.json` にコピーして **スクリプトID** を書く
（Apps Scriptの「プロジェクトの設定」に出ている英数字の長い文字列）。

```bash
cp .clasp.json.sample .clasp.json
# エディタで scriptId を書き換えてから
npx @google/clasp push -f
```

続けてウェブアプリとして公開する。実行ユーザーとアクセス範囲は
`gas/appsscript.json` に書いてあるので、コマンドだけで所定の設定になる。

```bash
npx @google/clasp create-deployment -d "初回"
npx @google/clasp list-deployments      # 出てきたIDが exec URL の一部になる
```

`https://script.google.com/macros/s/＜デプロイID＞/exec` が接続先URL。

**コードを直したあと**は次の2つ。URLは変わらない。

```bash
npx @google/clasp push -f
npx @google/clasp update-deployment ＜デプロイID＞
```

#### A-3. clasp の場合の初期化

`push` のあと、エディタで関数 **`setup`** を実行する。
初回の権限確認はブラウザでしか通せないので、ここだけは画面を開く。

```bash
npx @google/clasp open-script
```

実行ログに出るスプレッドシートURL・アプリキー・各人のPINを控える。

### B. スプレッドシート側の下ごしらえ

作られたスプレッドシートを開いて、

1. **シフト区分** … 早番・日勤・遅番・在宅・外勤・半休・公休・有給が最初から入っている。
   会社の勤務形態に合わせて、アプリの「シフト」タブか直接この表で直す。
2. **社員マスター** … 氏名とメールアドレスを正しく直す。
   初期メンバーは音声メモからの**仮登録**なので、必ず確認する。
   メールアドレスが空の人には通知が届かない。
3. **設定** … `事業所緯度` `事業所経度` を入れる。
   Googleマップで事業所を右クリック → 出てくる数字が「緯度, 経度」。
   `事業所半径(m)` の内側なら「事業所内」と判定される。
4. 通知を Slack 等にも流すなら `Webhook URL` に受信Webhookを入れる。

### C. アプリ（PWA）を公開する

`kintai/` はただの静的ファイルなので、置き場所はどちらでもよい。

**Cloudflare Pages（おすすめ・国内で速い）**

1. Cloudflare にログイン → Workers & Pages → Create → Pages → Connect to Git
2. このリポジトリを選ぶ
3. ビルド設定
   - Framework preset: **None**
   - Build command: **空欄**
   - Build output directory: **`kintai`**
4. デプロイすると `https://<プロジェクト名>.pages.dev/` で開ける
5. 独自ドメインを当てるなら Custom domains から

**GitHub Pages（このリポジトリで既に使っている）**

Settings → Pages を有効にすれば `https://<ユーザー名>.github.io/ai-obiwan/kintai/` で開ける。
追加の設定はいらない（`_headers` は Cloudflare 用なので無視される）。

### D. スマホに入れる

1. 上のURLをスマホのブラウザで開く
2. 最初の画面に **APIのURL**（手順Aの `/exec`）と**アプリキー**を入れて「接続する」
   - URLの後ろに `?api=<URL>&key=<キー>` を付けたリンクを配れば自動で入る。
     このリンクをQRコードにして配ると全員分の初期設定が一瞬で終わる。
3. 名前を選んでPINでログイン
4. ホーム画面に追加する
   - **iPhone（Safari）**: 共有ボタン → 「ホーム画面に追加」
   - **Android（Chrome）**: メニュー → 「アプリをインストール」
5. 位置情報の許可を聞かれたら**許可**する（出社の打刻に必要）

> iPhone は Safari で開いたときだけホーム画面に追加できる。Chrome アプリからだと出ない。

---

## 使い方

### シフトを組む（管理者）

「シフト」タブ → 月を選ぶ → **置きたいシフトをパレットで選んでから、マスを押す**。

- **日付（列の見出し）を押す** … その日を全員まとめて塗る（全員休みの日など）
- **名前を押す** … その人の**平日**をまとめて塗る
- **同じマスをもう一度押す** … 塗る前に戻す
- **先月から流用** … 4週間前の並びを、まだ空いている日に写す。曜日がそろう
- 塗っただけでは保存されない。黄色い枠が付いたマスが未保存で、「保存」で確定する

保存すると勤務予定に展開され、打刻の判定にも月次集計にもそのまま効く。
表の下に「出勤人数」の行と、ひとりあたりの勤務日数・休み・予定実働が出る。

一般社員も「シフト」タブを開けるが、見えるのも触れるのも自分の行だけ。

### シフト区分を決める（管理者）

「シフト」タブの一番下。記号（1〜2文字）・名称・開始・終了・休憩・勤務形態・区分・色を決める。
初期値は 早/日/遅/宅/外/半/休/有 の8種類。夜勤を足すなら「＋ 追加」から。
休憩をここで決めると、実働の計算からその分が引かれる。

### 予定を登録する（自分で入れる場合）

「予定」タブ → カレンダーで日をタップして選ぶ（何日でも選べる）→
シフトを選ぶか時間を直接入れて「この内容で登録」。
「平日を全部選ぶ」を押せばその月の月〜金がまとめて選ばれる。
有給・公休を選ぶと時間欄は使わない。同じ日にもう一度登録すると上書きになる。

画面の一番上が**月間予定**で、その月の出勤日数・休み・予定の実働が出る。
カレンダーの各日にはシフト記号が色つきで並ぶ。

### 打刻する

「ホーム」タブ → 勤務形態を選ぶ → 大きな「出勤」／「退勤」を押すだけ。

- **出社** … 位置情報が取れないと打刻できない（不正防止）
- **リモート** … 位置情報なしでも打刻できる。取れたときは記録する
- **外出・直行直帰** … 位置情報が取れれば記録し、取れなくても打刻できる

打刻するとその場で判定が付く。

| 判定 | 意味 |
|---|---|
| 予定どおり | 遅刻も早退もなし |
| 遅刻 | 出勤予定 +猶予（初期値5分）より遅い |
| 早退 | 退勤予定より早い |
| 予定外出勤 | その日の予定が無いのに打刻した |
| 欠勤 | 予定があったのに打刻がないまま日をまたいだ |

実働は「退勤 − 出勤 − 休憩」。休憩は実働が6時間を超えたときに引く。
引く分数は**シフト区分の休憩**が最優先で、無ければ社員ごとの所定休憩（初期値60分）。
残業は「実働 − 予定の実働」。数字はすべて設定シートで変えられる。

### 月次の統計

「統計」タブ。出勤日数・総実働・残業・遅刻・早退・欠勤に加えて、
**働き方の内訳とリモート率**が帯グラフで出る。
管理者が「全員」を選ぶとメンバー別の一覧表も出る（平均出勤時刻・平均退勤時刻つき）。

### 社員が増えたとき / 辞めたとき

「設定」タブ（管理者のみ）→ 社員 → 「＋ 追加」。
PINを空にすれば4桁が自動で発行され、追加後にトースト表示される。本人に伝える。
辞めた人は編集画面で**「在籍中」のチェックを外す**。行は消えないので過去の記録と集計はそのまま残る。

---

## データの中身

| シート | 役割 |
|---|---|
| 社員マスター | 社員ID・氏名・メール・PIN・権限・標準勤務時間・在籍 |
| 勤務予定 | 1人1日1行。シフト記号/出勤予定/退勤予定/勤務形態/区分 |
| シフト区分 | 早番・日勤・遅番などの型。記号/時刻/休憩/勤務形態/区分/色 |
| 出退勤記録 | 1人1日1行。予定と実績、判定、遅刻・早退・実働・残業、位置情報、端末 |
| 端末 | 端末ID・端末名・プラットフォーム・最終利用日時 |
| 通知ログ | いつ誰に何を送ったか |
| 設定 | 事業所座標、猶予分、通知のON/OFF、Webhook URL |

スプレッドシートを直接直しても構わない（打刻の修正など）。
ただし**1行目の見出しは触らないこと**。列の名前でデータを読んでいる。

---

## セキュリティについて

- ログインは「社員を選ぶ + PIN」。PINを8回続けて間違えると10分ロックする
- APIは**アプリキー**が合わないと応答しない
- ログイン状態は30日で切れる
- PINは社員マスターに平文で入っている。**このスプレッドシートを社外に共有しないこと**
- 位置情報は打刻したその瞬間の1点だけを記録する。常時追跡はしない
- 端末IDはブラウザごとに発行する乱数で、個人を特定する情報は含まない

---

## 動作確認

サーバー側のロジックは Google に上げなくても Node で丸ごと動かせる。

```bash
node attendance-app/test/run.js
```

打刻・遅刻早退の計算・シフト表・未打刻アラート・日次締め・統計・権限まわりなど144項目を通しで確認する。
貼り付け用の1枚版が最新かどうかもここで見ている。

1枚版そのものを動かして確かめることもできる。

```bash
node attendance-app/tools/bundle.js
mkdir -p /tmp/bundle && cp attendance-app/paste-to-gas.gs /tmp/bundle/
GAS_DIR=/tmp/bundle node attendance-app/test/run.js
```
`attendance-app/test/harness.js` が Apps Script のサービスをメモリ上で模擬している。

---

## よくあるつまずき

| 症状 | 原因と対処 |
|---|---|
| 「アプリキーが違います」 | 設定画面の入力ミス。GASの「プロジェクトの設定 > スクリプト プロパティ」の `APP_KEY` と見比べる |
| 「サーバーに接続できませんでした」 | デプロイの「アクセスできるユーザー」が**全員**になっているか確認 |
| コードを直したのに変わらない | claspなら `update-deployment`、手貼りなら「デプロイを管理」で**新バージョン**にする |
| `clasp push` が失敗する | <https://script.google.com/home/usersettings> で Apps Script API がオフ。オンにする |
| `clasp login` が通らない | 会社アカウントで管理者が外部アプリを制限している場合がある。個人アカウントか、管理者に確認 |
| 位置情報が取れない | ブラウザとOSの両方で位置情報を許可する。iPhoneは 設定 > プライバシー > 位置情報サービス > Safari |
| 通知が来ない | 社員マスターのメールアドレスが空。設定シートの「メール通知」がOFF。GASの1日あたり送信上限（無料枠100通）も確認 |
| シフト表を保存できない | 1回で保存できるのは400マスまで。月を分けて保存する |
| 打刻を間違えた | スプレッドシートの出退勤記録で時刻を直す。判定の再計算が要るときは管理者が予定を登録し直すと連動する |
| 画面が古いまま | ホーム画面のアプリを一度閉じて開き直す。ブラウザなら再読み込み |
